'use strict';

const book = {
	title: 'The Mountain is you',
	author: 'Brianna Wiest',
	year: 2020,
};

const { title, author, year } = book;

console.log(title);
console.log(author);
console.log(year);
